# Tasks: Add Configuration Guidance Comments

- [x] Add guidance comment above `$expectedNtpServers` in `Detect-LazyTime.ps1`
- [x] Add guidance comment above `$ntpServers` in `Set-LazyTime.ps1`
- [x] Add guidance comment above `$ntpServers` in `Test-NTP.ps1`
